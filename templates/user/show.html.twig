{% extends 'base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}
	<body class="home">
		<div class="container">
			<ol class="breadcrumb">
				<li>
					<a href="{{ path('accueil') }}">
						Accueil
					</a>
				</li>
				<li class="active">
					Mon profil
				</li>
			</ol>
			<div class="Nom-asso1">
				<h1 class="Nom-asso2">
					Mon profil
				</h1>
			</div>
			<table class="table">
				<tbody>
					<tr>
						<th>Nom</th>
						<td>{{ user.username }}</td>
					</tr>
					<tr>
						<th>Prénom</th>
						<td>{{ user.lastname }}</td>
					</tr>
					<tr>
						<th>Adresse mail</th>
						<td>{{ user.email }}</td>
					</tr>
					<tr>
						<th>Téléphone</th>
						<td>{{ user.tel }}</td>
					</tr>
				</tbody>
			</table>
			<a href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
			{{ include('user/_delete_form.html.twig') }}</div></br>
	</body>
</div>
</body>
<div id='scroll_to_top' class='opacity'>
<img src="{{ asset('images/collapse_arrow.svg') }}" alt="Retourner en haut">
</div><script src="{{ asset('js/menu.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/jQuery_lib.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/jQuery.js') }}"></script>
{% endblock %}
